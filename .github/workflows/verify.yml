name: Verify library & Generate documentation

on:
  push:
    branches:
    - main
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        pip install -U pip
        pip install -r requirements.txt

    - run: git switch main

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 'latest'
        cache: 'npm'

    - name: Install Vivliostyle
      run: npm install -g @vivliostyle/cli clang-format

    - name: Install dependencies
      run: npm install

    - name: Build PDF
      run: make build
    
    - name: Configure Git
      run: |
        git config --global user.email "kassyan323@gmail.com"
        git config --global user.name "shirotsume"

    - name: Commit PDF
      run: git add . && git commit -m "[skip actions] build PDF" && git push

    - name: Copy to gh-pages
      run: |
        DIR=$(mktemp -d)
        cp notebook.pdf $DIR
        cp build/*.css $DIR
        cp build/*.html $DIR
        git clean -dfx
        git switch gh-pages
        cp $DIR/* .
        git add . && git commit -m "build PDF" && git push
        git switch main
